{
  "name": "@sustainablesource/oracle",
  "version": "0.1.0",
  "scripts": {
    "test": "jest",
    "deploy": "now --token $NOW_TOKEN && now alias --token $NOW_TOKEN"
  },
  "devDependencies": {
    "@babel/plugin-transform-runtime": "^7.9.6",
    "@babel/preset-env": "^7.9.6",
    "jest": "^26.0.1",
    "now": "^19.0.1",
    "now-we-test": "^0.9.0"
  },
  "dependencies": {
    "@babel/runtime": "^7.9.6"
  }
}
